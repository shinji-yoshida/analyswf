<h1><%= @series.name %> シリーズ</h1>
<table class="table table-striped table-bordered">
  <tr><td>タイトル</td><td><%= @series.name %></td></tr>
</table>

<%= link_to 'swf 一覧', swfs_url(swf_series_id: @series.id), class: 'btn btn-info' %><%= link_to 'リソース一覧', test_resources_url(swf_series_id: @series.id), class: 'btn btn-info' %><%= link_to 'テストスクリプト編集', edit_swf_series_url(@series.id), class: 'btn btn-warning' %><br>
<br>

<table class="table table-striped table-bordered">
  <thead>
    <tr><td>識別子</td><td>タイプ</td></tr>
  </thead>
  <tbody>
    <%- @series.targets.each do |key, value| -%>
      <tr><td><%= key %></td><td><%= value %></td></tr>
    <%- end -%>
  </tbody>
</table>